<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="sdk_clicktopay_webview_main" />

    <ProgressBar
        android:id="@+id/progressBar"
        android:contentDescription="sdk_clicktopay_progress_loading"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:max="100"
        android:progress="0"
        android:visibility="gone" />

    <!-- Container for Mastercard DCF popup WebView -->
    <FrameLayout
        android:id="@+id/popupContainer"
        android:contentDescription="sdk_clicktopay_container_popup"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"
        android:background="@android:color/white" />

</FrameLayout>
